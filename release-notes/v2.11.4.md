<!-- date: 2026-04-08 -->
## v2.11.4

- **voice**: Guaranteed orphan grace period with per-user disconnect timestamps, replacing unreliable fixed-ticker sweep. WS reconnect uses exponential backoff (1.5s base) instead of flat 10s delay
- **voice**: LiveKit webhook handler for participant join/leave events with disconnect reason logging. Admin log panel now shows usernames (LEFT JOIN) and uses full-width layout
- **voice**: Restore screen share subscriptions after LiveKit reconnect
- **voice**: Added adaptiveStream and dynacast to LiveKit room options for bandwidth optimization
