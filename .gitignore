# ========================
# Go Backend
# ========================
server/mqvi
server/mqvi.exe
server/tmp/
server/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# ========================
# Node / Frontend
# ========================
client/node_modules/
client/dist/
client/.vite/
client/.tsbuildinfo

# ========================
# Electron
# ========================
dist-electron/
release/

# ========================
# Native (WASAPI audio capture)
# ========================
native/*.exe
native/*.obj
*.obj

# ========================
# Root node_modules (Electron + build tools)
# ========================
/node_modules/

# ========================
# Environment & Secrets
# ========================
.env
.env.local
.env.production
*.pem
*.key

# Push notifications / Firebase secrets (NEVER commit)
*.p8
firebase-service-account*.json
server/firebase-service-account.json
# Firebase client config — git-ignored because the repo is public. Not strict
# secrets (package-restricted, shipped inside the app) but exposing a real
# project's config publicly invites abuse. Keep locally; inject via CI secret
# when Android/iOS CI builds are set up.
client/android/app/google-services.json
client/ios/App/App/GoogleService-Info.plist

# ========================
# IDE
# ========================
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# ========================
# OS
# ========================
.DS_Store
Icon?
Thumbs.db
desktop.ini

# ========================
# Server runtime data
# ========================
/server/data/
/uploads/

# ========================
# Documents (private)
# ========================
documents/
# ========================
# MD files (private)
# ========================
docs/

# ========================
# Deploy build output (contains compiled binary + real configs)
# ========================
deploy/package/
deploy/COMMANDS.md
deploy/REDEPLOY.md

# ========================
# Deploy scripts with real server IP (use .example versions)
# ========================
deploy/redeploy.sh
deploy/redeploy.ps1
# ========================
# Deploy scripts with real server IP (use .example versions)
# ========================
deploy/redeploy-prod.sh
deploy/redeploy-prod.ps1

# ========================
# Embedded frontend build
# ========================
server/static/dist/
!server/static/dist/.gitkeep

# ========================
# One-time migration tools
# ========================
server/cmd/
server/migrate-uploads.exe
deploy/migrate-uploads
deploy/migrate-uploads.ps1

# ========================
# Misc
# ========================
*.log
*.tmp
coverage/
deploy.md


# ========================
# claude
# ========================
*.claude
CLAUDE.md
DECISIONS.md
